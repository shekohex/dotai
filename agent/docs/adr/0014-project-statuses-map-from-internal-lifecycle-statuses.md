# Project statuses map from internal lifecycle statuses

Pi Conductor will use an internal lifecycle status vocabulary: `draft`, `ready`, `in_progress`, `in_review`, `done`, and `blocked`. Each managed GitHub Project can map those statuses to its own option names, with defaults available so simple boards work without full configuration. Defaults are `Draft`, `Todo`, `In Progress`, `Review`, `Done`, and `Blocked` respectively.
