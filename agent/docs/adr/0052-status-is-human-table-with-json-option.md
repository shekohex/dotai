# Status is human table with JSON option

`pi conductor status` will print a concise human-readable table by default and support `--json` for machine-readable state. Other inspection commands should follow the same default-human, optional-JSON pattern where useful.
