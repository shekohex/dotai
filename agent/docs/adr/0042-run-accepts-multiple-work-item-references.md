# Run accepts multiple work item references

`pi conductor run` will accept a GitHub issue URL, `owner/repo#number`, a current-repository issue number, or a GitHub Project item ID. The command resolves the reference to the configured managed repository and project before dispatching.
