# Implementation Plans

Generated by the improve skill on 2026-07-10. Execute in order. Each executor must read its plan fully, honor STOP conditions, and update this table after completing or blocking work.

## Execution order and status

| Plan | Title                                           | Priority | Effort | Depends on | Status      |
| ---- | ----------------------------------------------- | -------- | ------ | ---------- | ----------- |
| 001  | Host selected Pi Conductor runs through T3 HTTP | P1       | L      | none       | IN PROGRESS |

Status values: TODO | IN PROGRESS | DONE | BLOCKED with reason | REJECTED with rationale.

## Dependency notes

- Plan 001 changes T3's direct HTTP contract first, then consumes it from Pi Conductor. Do not merge Dotai T3-session code against a T3 server that lacks the advertised capability and delivery behavior.
- T3's current Pi-provider files have uncommitted work. Coordinate before starting Plan 001; do not overwrite or fold unrelated work into this plan.

## Existing unnumbered notes

- `plannotator-review-mode-integration.md` is an unrelated pre-existing plan.
- `plannotator-v0.19.12-vendor-upgrade-assessment.md` is an unrelated historical assessment.

## Findings considered and rejected

- T3 CLI bridge: rejected. Direct authenticated HTTP already exposes required project, thread, session, and dispatch operations; a CLI would duplicate protocol/authentication behavior.
- WebSocket-first client: rejected. HTTP snapshots plus existing Conductor polling meet correctness requirements; subscriptions can be a later optimization.
- Encode T3 identifiers into Herdr workspace/tab/pane fields: rejected. It destroys durable host semantics and makes recovery ambiguous.
- Replace all Herdr integrations: rejected. This plan changes Pi Conductor session hosting only; subagent, CoreUI, and background-shell Herdr features stay independent.
- Silent fallback from unavailable T3 to Herdr: rejected. It makes session location unpredictable and can create duplicate Pi agents.
