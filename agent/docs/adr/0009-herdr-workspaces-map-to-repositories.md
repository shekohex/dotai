# Herdr workspaces map to repositories

Pi Conductor will manage multiple Herdr workspaces through the Herdr CLI, with one Herdr workspace per repository and one tab per active issue or pull request. This keeps all sessions for a repository grouped together while preserving a visible, steerable terminal context per work item.
