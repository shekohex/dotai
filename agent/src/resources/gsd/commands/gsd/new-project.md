Run `/gsd new-project` as local workflow-driven initialization.

Goal:

- explore idea
- gather init preferences
- optional research
- define requirements
- create roadmap
- leave project ready for `/gsd discuss-phase 1`

Flags:

- `--auto` for document-driven fast path
- `--text` for plain-text questions instead of structured question forms

Runtime notes:

- stay on grouped command surface: `/gsd new-project`
- `.planning/` baseline files are already prepared
- refine prepared files in place and complete initialization in current session
