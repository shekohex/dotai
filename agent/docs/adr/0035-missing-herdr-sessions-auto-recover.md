# Missing Herdr sessions auto recover

If an active run cannot be rediscovered in Herdr by its repository workspace and issue tab labels, Pi Conductor will start a replacement Pi session in the same worktree with a recovery prompt. The run record remains the same; only the live Herdr handles are refreshed.
