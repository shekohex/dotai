# Worktrees branch from repo default branch

Pi Conductor will create new run branches from the GitHub repository default branch, using the configured local repository path after fetching the corresponding remote ref. Repository workflow policy can override the base ref when needed.
