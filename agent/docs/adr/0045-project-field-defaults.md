# Project field defaults

Pi Conductor will assume GitHub Project field names `Status`, `Effort`, and `Priority` by default. Managed project configuration can override those names when a board uses different field labels.
