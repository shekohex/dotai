diff --git a/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/components/tool-execution.js b/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/components/tool-execution.js
index b57b6ae..3408d50 100644
--- a/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/components/tool-execution.js
+++ b/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/components/tool-execution.js
@@ -39,7 +39,6 @@ export class ToolExecutionComponent extends Container {
         this.imageWidthCells = options.imageWidthCells ?? 60;
         this.ui = ui;
         this.cwd = cwd;
-        this.addChild(new Spacer(1));
         // Always create all shell variants. contentBox is used for default renderer-based composition.
         // selfRenderContainer is used when the tool renders its own framing.
         // contentText is reserved for generic fallback rendering when no tool definition exists.
diff --git a/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.d.ts b/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.d.ts
index 01919db..6122ae3 100644
--- a/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.d.ts
+++ b/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.d.ts
@@ -4,6 +4,7 @@
  */
 import { type ImageContent } from "@mariozechner/pi-ai";
 import { type AgentSessionRuntime } from "../../core/agent-session-runtime.js";
+import type { SessionInfo, SessionListProgress } from "../../core/session-manager.js";
 export declare function isApiKeyLoginProvider(providerId: string, oauthProviderIds: ReadonlySet<string>, builtInProviderIds?: ReadonlySet<string>): boolean;
 export declare function getApiKeyProviderDisplayName(providerId: string): string;
 /**
@@ -22,6 +23,13 @@ export interface InteractiveModeOptions {
     initialMessages?: string[];
     /** Force verbose startup (overrides quietStartup setting) */
     verbose?: boolean;
+    /** Optional override for session resume picker data sources and mutations */
+    sessionSelectorLoaders?: {
+        currentSessionsLoader: (onProgress?: SessionListProgress) => Promise<SessionInfo[]>;
+        allSessionsLoader: (onProgress?: SessionListProgress) => Promise<SessionInfo[]>;
+        renameSession?: ((sessionPath: string, nextName: string | undefined) => Promise<void>) | null;
+        showRenameHint?: boolean;
+    };
 }
 export declare class InteractiveMode {
     private options;
diff --git a/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.js b/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.js
index 468f916..aa5f8f3 100644
--- a/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.js
+++ b/node_modules/@mariozechner/pi-coding-agent/dist/modes/interactive/interactive-mode.js
@@ -118,6 +118,36 @@ export function isApiKeyLoginProvider(providerId, oauthProviderIds, builtInProvi
 export function getApiKeyProviderDisplayName(providerId) {
     return API_KEY_LOGIN_PROVIDERS[providerId] ?? providerId;
 }
+function getVisibleChatChildKind(component, width) {
+    if (component instanceof Spacer) {
+        return undefined;
+    }
+    if (component instanceof ToolExecutionComponent) {
+        return "tool";
+    }
+    if (component instanceof Text || component instanceof Markdown) {
+        return component.text.trim() ? "other" : undefined;
+    }
+    const renderedLines = component.render(width);
+    return renderedLines.some((line) => visibleWidth(line.trim()) > 0) ? "other" : undefined;
+}
+function getLastVisibleChatChildKind(interactiveMode) {
+    const width = interactiveMode.ui.terminal.columns;
+    for (let index = interactiveMode.chatContainer.children.length - 1; index >= 0; index--) {
+        const kind = getVisibleChatChildKind(interactiveMode.chatContainer.children[index], width);
+        if (kind) {
+            return kind;
+        }
+    }
+    return undefined;
+}
+function addToolExecutionToChat(interactiveMode, component) {
+    const previousKind = getLastVisibleChatChildKind(interactiveMode);
+    if (previousKind && previousKind !== "tool") {
+        interactiveMode.chatContainer.addChild(new Spacer(1));
+    }
+    interactiveMode.chatContainer.addChild(component);
+}
 export class InteractiveMode {
     options;
     runtimeHost;
@@ -2194,7 +2224,7 @@ export class InteractiveMode {
                                     imageWidthCells: this.settingsManager.getImageWidthCells(),
                                 }, this.getRegisteredToolDefinition(content.name), this.ui, this.sessionManager.getCwd());
                                 component.setExpanded(this.toolOutputExpanded);
-                                this.chatContainer.addChild(component);
+                                addToolExecutionToChat(this, component);
                                 this.pendingTools.set(content.id, component);
                             }
                             else {
@@ -2255,7 +2285,7 @@ export class InteractiveMode {
                         imageWidthCells: this.settingsManager.getImageWidthCells(),
                     }, this.getRegisteredToolDefinition(event.toolName), this.ui, this.sessionManager.getCwd());
                     component.setExpanded(this.toolOutputExpanded);
-                    this.chatContainer.addChild(component);
+                    addToolExecutionToChat(this, component);
                     this.pendingTools.set(event.toolCallId, component);
                 }
                 component.markExecutionStarted();
@@ -2533,7 +2563,7 @@ export class InteractiveMode {
                             imageWidthCells: this.settingsManager.getImageWidthCells(),
                         }, this.getRegisteredToolDefinition(content.name), this.ui, this.sessionManager.getCwd());
                         component.setExpanded(this.toolOutputExpanded);
-                        this.chatContainer.addChild(component);
+                        addToolExecutionToChat(this, component);
                         if (message.stopReason === "aborted" || message.stopReason === "error") {
                             let errorMessage;
                             if (message.stopReason === "aborted") {
@@ -3527,7 +3557,19 @@ export class InteractiveMode {
     }
     showSessionSelector() {
         this.showSelector((done) => {
-            const selector = new SessionSelectorComponent((onProgress) => SessionManager.list(this.sessionManager.getCwd(), this.sessionManager.getSessionDir(), onProgress), SessionManager.listAll, async (sessionPath) => {
+            const currentSessionsLoader = this.options.sessionSelectorLoaders?.currentSessionsLoader ??
+                ((onProgress) => SessionManager.list(this.sessionManager.getCwd(), this.sessionManager.getSessionDir(), onProgress));
+            const allSessionsLoader = this.options.sessionSelectorLoaders?.allSessionsLoader ?? SessionManager.listAll;
+            const renameSession = this.options.sessionSelectorLoaders?.renameSession === undefined
+                ? async (sessionFilePath, nextName) => {
+                    const next = (nextName ?? "").trim();
+                    if (!next)
+                        return;
+                    const mgr = SessionManager.open(sessionFilePath);
+                    mgr.appendSessionInfo(next);
+                }
+                : this.options.sessionSelectorLoaders.renameSession ?? undefined;
+            const selector = new SessionSelectorComponent(currentSessionsLoader, allSessionsLoader, async (sessionPath) => {
                 done();
                 await this.handleResumeSession(sessionPath);
             }, () => {
@@ -3536,14 +3578,8 @@ export class InteractiveMode {
             }, () => {
                 void this.shutdown();
             }, () => this.ui.requestRender(), {
-                renameSession: async (sessionFilePath, nextName) => {
-                    const next = (nextName ?? "").trim();
-                    if (!next)
-                        return;
-                    const mgr = SessionManager.open(sessionFilePath);
-                    mgr.appendSessionInfo(next);
-                },
-                showRenameHint: true,
+                renameSession,
+                showRenameHint: this.options.sessionSelectorLoaders?.showRenameHint ?? true,
                 keybindings: this.keybindings,
             }, this.sessionManager.getSessionFile());
             return { component: selector, focus: selector };
