# Repo workflow policy lives under .pi

Repository-owned Pi Conductor policy will live in `.pi/WORKFLOW.md`, not at the repository root. The file uses YAML frontmatter for repository policy such as branch template, verification expectations, PR expectations, and cleanup policy, plus a Markdown body for the initial Pi prompt template; this keeps agent workflow configuration grouped with other Pi project resources.
