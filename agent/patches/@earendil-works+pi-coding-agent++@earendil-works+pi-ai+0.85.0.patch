diff --git a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js
index bf979d4..0b0223b 100644
--- a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js
+++ b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js
@@ -173,6 +173,9 @@ export const stream = (model, context, options) => {
             if (nextBody !== undefined) {
                 body = nextBody;
             }
+            const responseOptions = body.service_tier === undefined
+                ? options
+                : { ...options, serviceTier: body.service_tier };
             const websocketRequestId = codexSessionId || uuidv7();
             const sseHeaders = buildSSEHeaders(model.headers, options?.headers, accountId, apiKey, codexSessionId);
             const websocketHeaders = buildWebSocketHeaders(model.headers, options?.headers, accountId, apiKey, websocketRequestId);
@@ -198,7 +201,7 @@ export const stream = (model, context, options) => {
                                 startEmitted = true;
                                 stream.push({ type: "start", partial: output });
                             }
-                        }, httpTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, accountId, grammarToolInputProperties, options);
+                        }, httpTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, accountId, grammarToolInputProperties, responseOptions);
                         if (options?.signal?.aborted) {
                             throw new Error("Request was aborted");
                         }
@@ -327,7 +330,7 @@ export const stream = (model, context, options) => {
                 startEmitted = true;
                 stream.push({ type: "start", partial: output });
             }
-            await processStream(response, output, stream, model, grammarToolInputProperties, options);
+            await processStream(response, output, stream, model, grammarToolInputProperties, responseOptions);
             if (options?.signal?.aborted) {
                 throw new Error("Request was aborted");
             }
@@ -1271,7 +1274,8 @@ function buildBaseCodexHeaders(initHeaders, additionalHeaders, accountId, token)
     }
     headers.set("Authorization", `Bearer ${token}`);
     headers.set("chatgpt-account-id", accountId);
-    headers.set("originator", "pi");
+    if (!headers.has("originator"))
+        headers.set("originator", "pi");
     headers.set("User-Agent", getPiUserAgent());
     return headers;
 }
diff --git a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
index 2605022..e2bf040 100644
--- a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
+++ b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
@@ -119,6 +119,7 @@ export const stream = (model, context, options) => {
             await processResponsesStream(openaiStream, output, stream, model, {
                 serviceTier: options?.serviceTier,
                 grammarToolInputProperties,
+                resolveServiceTier: resolveRequestedServiceTier,
                 applyServiceTierPricing: (usage, serviceTier) => applyServiceTierPricing(usage, serviceTier, model),
             });
             if (options?.signal?.aborted) {
@@ -275,6 +276,11 @@ function getServiceTierCostMultiplier(model, serviceTier) {
             return 1;
     }
 }
+function resolveRequestedServiceTier(responseServiceTier, requestServiceTier) {
+    if (responseServiceTier === "default" && (requestServiceTier === "flex" || requestServiceTier === "priority"))
+        return requestServiceTier;
+    return responseServiceTier ?? requestServiceTier;
+}
 function applyServiceTierPricing(usage, serviceTier, model) {
     const multiplier = getServiceTierCostMultiplier(model, serviceTier);
     if (multiplier === 1)
diff --git a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
index 216a45f..f81f025 100644
--- a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
+++ b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
@@ -13,6 +13,7 @@ export function buildBaseOptions(model, context, options, apiKey) {
         : undefined;
     return {
         temperature: options?.temperature,
+        serviceTier: options?.serviceTier,
         samplingParams,
         maxTokens: clampMaxTokensToContext(model, context, options?.maxTokens ?? model.maxTokens),
         signal: options?.signal,
diff --git a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/utils/retry.js b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/utils/retry.js
index ce8c4cb..589ba67 100644
--- a/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/utils/retry.js
+++ b/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/utils/retry.js
@@ -44,6 +44,9 @@ const RETRYABLE_PROVIDER_ERROR_PATTERN = buildProviderErrorPattern([
     "connection.?lost",
     "other side closed",
     "fetch failed",
+    "request failed",
+    "failed after retries",
+    "no response body",
     "getaddrinfo",
     "ENOTFOUND",
     "EAI_AGAIN",
@@ -57,11 +60,13 @@ const RETRYABLE_PROVIDER_ERROR_PATTERN = buildProviderErrorPattern([
     // WebSocket transports can report close/error text instead of HTTP/fetch text.
     "websocket.?closed",
     "websocket.?error",
+    "websocket transport is not available",
     // Premature stream endings from SDKs and transports. Anthropic can throw
     // "stream ended without ..." and "Anthropic stream ended before message_stop"
     // (#4433); Bedrock/Smithy can throw an HTTP/2 no-response error (#3594).
     "ended without",
     "stream ended before message_stop",
+    "stream closed before response\\.completed",
     "stream ended before a terminal response event",
     "http2 request did not get a response",
     // Provider-requested retry delay cap failures should flow through the outer
@@ -69,6 +74,10 @@ const RETRYABLE_PROVIDER_ERROR_PATTERN = buildProviderErrorPattern([
     "retry delay",
     // Explicit retry guidance emitted mid-stream by OpenAI Responses and Bedrock
     // stream exceptions (#6019).
+    "invalid codex sse json",
+    "invalid codex websocket json",
+    "error occurred while processing your request",
+    "response api in-stream error",
     "you can retry your request",
     "try your request again",
     "please retry your request",
