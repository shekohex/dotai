# Local CLI comes before GitHub comment commands

The first Pi Conductor command surface will be local `pi conductor ...` commands only. GitHub `/pi ...` comment commands are deferred until core run state, dispatch, and Herdr/Pi control are stable, reducing permission and webhook complexity in v1.
