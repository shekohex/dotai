# First slice includes the local command set

Pi Conductor will not start with only a daemon or one-shot reconciler. The first implementation slice includes the local command router surface so operators can serve, reconcile, inspect, send handoffs, and stop runs from the terminal while Herdr remains the live session console.
