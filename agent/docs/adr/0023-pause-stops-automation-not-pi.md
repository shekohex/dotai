# Pause stops automation not Pi

`pi conductor pause <run>` will pause conductor automation for that run without stopping the Pi process or closing the Herdr tab. Operators can keep steering the visible Pi session manually, and `resume` re-enables automated follow-ups and lifecycle mutations.
