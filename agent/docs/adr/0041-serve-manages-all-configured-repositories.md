# Serve manages all configured repositories

`pi conductor serve` will scan and reconcile all managed repositories in global conductor configuration by default. Repo-scoped flags can narrow command behavior later, but the daemon default is the full configured control plane.
