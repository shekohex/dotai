# First slice local commands

Pi Conductor's first local command surface will include `serve`, `reconcile`, `status`, `runs`, `run`, `logs`, `send`, `stop`, `pause`, `resume`, `retry`, `cleanup`, `config init`, and `config validate`. Project registration helpers and debug/recovery extras are deferred unless needed by these commands.
