diff --git a/node_modules/@mariozechner/pi-tui/dist/keys.js b/node_modules/@mariozechner/pi-tui/dist/keys.js
index 374f69a..e4de602 100644
--- a/node_modules/@mariozechner/pi-tui/dist/keys.js
+++ b/node_modules/@mariozechner/pi-tui/dist/keys.js
@@ -217,6 +217,13 @@ const KITTY_FUNCTIONAL_KEY_EQUIVALENTS = new Map([
 function normalizeKittyFunctionalCodepoint(codepoint) {
     return KITTY_FUNCTIONAL_KEY_EQUIVALENTS.get(codepoint) ?? codepoint;
 }
+function normalizeKittyLetterCodepoint(codepoint, modifier) {
+    const effectiveModifier = modifier & ~LOCK_MASK;
+    if ((effectiveModifier & MODIFIERS.shift) !== 0 && codepoint >= 65 && codepoint <= 90) {
+        return codepoint + 32;
+    }
+    return codepoint;
+}
 const LEGACY_KEY_SEQUENCES = {
     up: ["\x1b[A", "\x1bOA"],
     down: ["\x1b[B", "\x1bOB"],
@@ -416,8 +423,15 @@ function parseKittySequence(data) {
         const baseLayoutKey = csiUMatch[3] ? parseInt(csiUMatch[3], 10) : undefined;
         const modValue = csiUMatch[4] ? parseInt(csiUMatch[4], 10) : 1;
         const eventType = parseEventType(csiUMatch[5]);
+        const modifier = modValue - 1;
         _lastEventType = eventType;
-        return { codepoint, shiftedKey, baseLayoutKey, modifier: modValue - 1, eventType };
+        return {
+            codepoint: normalizeKittyLetterCodepoint(codepoint, modifier),
+            shiftedKey,
+            baseLayoutKey,
+            modifier,
+            eventType,
+        };
     }
     // Arrow keys with modifier: \x1b[1;<mod>A/B/C/D or \x1b[1;<mod>:<event>A/B/C/D
     const arrowMatch = data.match(/^\x1b\[1;(\d+)(?::(\d+))?([ABCD])$/);
@@ -500,7 +514,8 @@ function parseModifyOtherKeysSequence(data) {
         return null;
     const modValue = parseInt(match[1], 10);
     const codepoint = parseInt(match[2], 10);
-    return { codepoint, modifier: modValue - 1 };
+    const modifier = modValue - 1;
+    return { codepoint: normalizeKittyLetterCodepoint(codepoint, modifier), modifier };
 }
 /**
  * Match xterm modifyOtherKeys format: CSI 27 ; modifiers ; keycode ~
@@ -874,7 +889,8 @@ export function matchesKey(data, keyId) {
         const isDigit = isDigitKey(key);
         if (ctrl && alt && !shift && !_kittyProtocolActive && rawCtrl) {
             // Legacy: ctrl+alt+key is ESC followed by the control character
-            return data === `\x1b${rawCtrl}`;
+            if (data === `\x1b${rawCtrl}`)
+                return true;
         }
         if (alt && !ctrl && !shift && !_kittyProtocolActive && (isLetter || isDigit)) {
             // Legacy: alt+letter/digit is ESC followed by the key
diff --git a/node_modules/@mariozechner/pi-tui/dist/tui.js b/node_modules/@mariozechner/pi-tui/dist/tui.js
index e23b801..0fccf1d 100644
--- a/node_modules/@mariozechner/pi-tui/dist/tui.js
+++ b/node_modules/@mariozechner/pi-tui/dist/tui.js
@@ -835,11 +835,25 @@ export class TUI extends Container {
             return;
         }
         // Differential rendering can only touch what was actually visible.
-        // If the first changed line is above the previous viewport, we need a full redraw.
+        // If changes happen above the viewport but line count is stable, avoid full redraws.
         if (firstChanged < prevViewportTop) {
-            logRedraw(`firstChanged < viewportTop (${firstChanged} < ${prevViewportTop})`);
-            fullRender(true);
-            return;
+            const lineCountUnchanged = newLines.length === this.previousLines.length;
+            if (lineCountUnchanged && lastChanged < prevViewportTop) {
+                this.positionHardwareCursor(cursorPos, newLines.length);
+                this.previousLines = newLines;
+                this.previousWidth = width;
+                this.previousHeight = height;
+                this.previousViewportTop = prevViewportTop;
+                return;
+            }
+            if (lineCountUnchanged) {
+                firstChanged = prevViewportTop;
+            }
+            else {
+                logRedraw(`firstChanged < viewportTop (${firstChanged} < ${prevViewportTop})`);
+                fullRender(true);
+                return;
+            }
         }
         // Render from first changed line to end
         // Build buffer with all updates wrapped in synchronized output
